<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.randomizerapp.randomizer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.Wallpaper">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AppsListActivity"
            android:theme="@style/AppTheme.Wallpaper"></activity>
        <activity
            android:name=".Grid"
            android:theme="@style/AppTheme.Wallpaper"></activity>
        <activity
            android:name=".GridActivity"
            android:theme="@style/AppTheme.Wallpaper"></activity>
        <activity
            android:name=".FolderActivity"
            android:theme="@style/AppTheme.Wallpaper"></activity>
        <activity
            android:name=".TabbedGrid"
            android:label="@string/title_activity_tabbed_grid"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>